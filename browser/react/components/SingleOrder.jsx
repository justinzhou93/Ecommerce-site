import React from 'react';

export default function (props) {
  return (
    <div>
      <h3>single orders page</h3>
      {/*<ul>
          {
            props.orders && props.orders.lineItems
            .map(lineItem => <li key={lineItem.id}>{lineItem}</li>)
          }
      </ul>*/}
    </div>
  )
}
